Issue Identification: The returned result does not print the fruit at the correct index.

Problem Analysis: The issue likely stems from incorrect indexing of the set elements.

Code Example:

name1 = id_to_fruit(1, {"apple", "orange", "melon", "kiwi", "strawberry"})
name3 = id_to_fruit(3, {"apple", "orange", "melon", "kiwi", "strawberry"})
name4 = id_to_fruit(4, {"apple", "orange", "melon", "kiwi", "strawberry"})


Solution Approach: To fix this, ensure that the index provided corresponds to the correct element in the set.
